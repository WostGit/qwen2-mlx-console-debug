name: qwen2-mlx-console-debug

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  preliminaries:
    runs-on: macos-latest
    timeout-minutes: 45

    env:
      PYTHONUNBUFFERED: "1"
      HF_HOME: ${{ github.workspace }}/.hf-cache
      HF_HUB_ENABLE_HF_TRANSFER: "1"
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
      MODEL_ID: Qwen/Qwen2-0.5B

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache Hugging Face files
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/.hf-cache
          key: ${{ runner.os }}-hf-${{ env.MODEL_ID }}
          restore-keys: |
            ${{ runner.os }}-hf-

      - name: System info
        shell: bash
        run: |
          set -euxo pipefail
          echo "=== SYSTEM INFO ==="
          sw_vers
          uname -a
          python --version
          which python
          echo "HF_HOME=$HF_HOME"
          if [ -n "${HF_TOKEN:-}" ]; then
            echo "HF_TOKEN_PRESENT=yes"
          else
            echo "HF_TOKEN_PRESENT=no"
          fi

      - name: Install dependencies
        shell: bash
        run: |
          set -euxo pipefail
          python -m pip install --upgrade pip wheel setuptools
          python -m pip install mlx mlx-lm huggingface_hub numpy pyyaml

      - name: Package versions
        shell: bash
        run: |
          set -euxo pipefail
          python - <<'PY'
          import importlib
          print("=== PACKAGE VERSIONS ===")
          for name in ["mlx", "mlx_lm", "huggingface_hub", "numpy", "yaml"]:
              m = importlib.import_module(name)
              print(f"{name}: {getattr(m, '__version__', 'unknown')}")
          PY

      - name: Hugging Face auth and cache check
        shell: bash
        run: |
          set -euxo pipefail
          python - <<'PY'
          import os
          from huggingface_hub import HfApi

          print("=== HF CHECK ===")
          token = os.environ.get("HF_TOKEN")
          print("HF_TOKEN_PRESENT =", bool(token))
          print("HF_HOME =", os.environ.get("HF_HOME"))

          api = HfApi(token=token if token else None)
          info = api.model_info("Qwen/Qwen2-0.5B")
          print("MODEL EXISTS =", info.id)
          PY

      - name: Qwen startup self-check
        shell: bash
        run: |
          set -euxo pipefail
          python - <<'PY'
          import os
          import platform
          import sys

          import mlx.core as mx
          from mlx_lm import load

          MODEL_ID = os.environ.get("MODEL_ID", "Qwen/Qwen2-0.5B")
          HF_TOKEN = os.environ.get("HF_TOKEN")
          HF_HOME = os.environ.get("HF_HOME")

          print("=== QWEN STARTUP SELF-CHECK ===")
          print("PYTHON =", sys.version)
          print("PLATFORM =", platform.platform())
          print("MODEL_ID =", MODEL_ID)
          print("HF_HOME =", HF_HOME)
          print("HF_TOKEN_PRESENT =", bool(HF_TOKEN))

          print("\nLoading model/tokenizer...")
          model, tokenizer = load(MODEL_ID)
          print("MODEL_CLASS =", type(model).__name__)
          print("TOKENIZER_CLASS =", type(tokenizer).__name__)

          vocab_size = getattr(tokenizer, "vocab_size", None)
          print("TOKENIZER_VOCAB_SIZE =", vocab_size)

          prompts = [
              "Hello world",
              "The capital of France is",
              "2 + 2 ="
          ]

          def tokenize(tok, text):
              if hasattr(tok, "encode"):
                  ids = tok.encode(text)
                  if ids and isinstance(ids[0], list):
                      ids = ids[0]
                  return list(ids)
              raise RuntimeError("Could not tokenize prompt with available tokenizer API")

          def extract_logits(out):
              if hasattr(out, "logits"):
                  return out.logits
              if isinstance(out, tuple) and len(out) > 0:
                  return out[0]
              return out

          for i, prompt in enumerate(prompts):
              print("\n--- PROMPT", i, "---")
              print("TEXT =", repr(prompt))
              ids = tokenize(tokenizer, prompt)
              print("TOKEN_IDS =", ids)
              print("TOKEN_IDS_TYPE =", type(ids).__name__)
              print("FIRST_TOKEN_TYPE =", type(ids[0]).__name__ if ids else None)
              assert all(isinstance(x, int) for x in ids), "Token ids must be Python ints"

              x = mx.array([ids], dtype=mx.int32)
              print("MLX_ARRAY_SHAPE =", x.shape)
              print("MLX_ARRAY_DTYPE =", x.dtype)

              out = model(x)
              logits = extract_logits(out)
              print("LOGITS_TYPE =", type(logits).__name__)
              print("LOGITS_SHAPE =", logits.shape)
              print("LOGITS_DTYPE =", getattr(logits, "dtype", None))

              if len(logits.shape) == 3:
                  next_logits = logits[0, -1, :]
              elif len(logits.shape) == 2:
                  next_logits = logits[-1, :]
              else:
                  raise RuntimeError(f"Unexpected logits shape: {logits.shape}")

              probs = mx.softmax(next_logits, axis=-1)
              probs32 = probs.astype(mx.float32)

              print("PROBS_SHAPE =", probs.shape)
              print("PROBS_DTYPE =", probs.dtype)
              print("PROBS32_DTYPE =", probs32.dtype)
              print("PROBS_SUM =", float(mx.sum(probs32).item()))

              # MLX topk returns values only in current environment
              top_vals = mx.topk(probs32, k=10)
              top_idx = mx.argsort(probs32, axis=-1)[-10:]
              top_idx = top_idx[::-1]

              print("TOP10_IDS =", [int(v) for v in top_idx.tolist()])
              print("TOP10_PROBS =", [float(v) for v in top_vals.tolist()])

              if vocab_size is not None:
                  print("VOCAB_MATCH =", probs.shape[-1] == vocab_size)
                  print("MODEL_VOCAB_SIZE =", probs.shape[-1])
                  if probs.shape[-1] != vocab_size:
                      print("WARNING: tokenizer vocab and logits vocab differ")

          print("\nQWEN_STARTUP_SELF_CHECK = PASS")
          PY

      - name: Toy interface experiment
        shell: bash
        run: |
          set -euxo pipefail
          python - <<'PY'
          import math

          print("=== TOY INTERFACE EXPERIMENT ===")

          def softmax(xs):
              m = max(xs)
              ex = [math.exp(x - m) for x in xs]
              s = sum(ex)
              return [x / s for x in ex]

          def kl(p, q, eps=1e-12):
              out = 0.0
              for pi, qi in zip(p, q):
                  qi = max(qi, eps)
                  pi = max(pi, eps)
                  out += pi * math.log(pi / qi)
              return out

          def argmax_view(p):
              j = max(range(len(p)), key=lambda i: p[i])
              out = [0.0] * len(p)
              out[j] = 1.0
              return out

          def topk_view(p, k=3):
              idx = sorted(range(len(p)), key=lambda i: p[i], reverse=True)[:k]
              out = [0.0] * len(p)
              s = sum(p[i] for i in idx)
              for i in idx:
                  out[i] = p[i] / s
              return out

          logits = [1.7, 0.9, 0.4, -0.2, -1.1]
          victim = softmax(logits)
          print("VICTIM =", victim)

          student_preds = {
              "argmax": argmax_view(victim),
              "topk": topk_view(victim, 3),
              "probs": list(victim),
          }

          for name, pred in student_preds.items():
              agreement = int(max(range(len(pred)), key=lambda i: pred[i]) == max(range(len(victim)), key=lambda i: victim[i]))
              div = kl(victim, pred)
              print(f"\nINTERFACE={name}")
              print("PRED =", pred)
              print("AGREEMENT =", agreement)
              print("KL =", div)
          PY

      - name: Qwen interface experiment
        shell: bash
        run: |
          set -euxo pipefail
          python - <<'PY'
          import os
          import mlx.core as mx
          from mlx_lm import load

          MODEL_ID = os.environ.get("MODEL_ID", "Qwen/Qwen2-0.5B")
          print("=== QWEN INTERFACE EXPERIMENT ===")
          print("MODEL_ID =", MODEL_ID)

          model, tokenizer = load(MODEL_ID)

          prompts = [
              "The capital of France is",
              "2 + 2 =",
              "The opposite of cold is",
          ]

          def tokenize(tok, text):
              if hasattr(tok, "encode"):
                  ids = tok.encode(text)
                  if ids and isinstance(ids[0], list):
                      ids = ids[0]
                  return list(ids)
              raise RuntimeError("Could not tokenize prompt")

          def extract_logits(out):
              if hasattr(out, "logits"):
                  return out.logits
              if isinstance(out, tuple) and len(out) > 0:
                  return out[0]
              return out

          def one_hot_from_top1(probs):
              idx = int(mx.argmax(probs).item())
              out = mx.zeros_like(probs)
              out[idx] = 1.0
              return out

          def topk_view(probs, k=5):
              vals = mx.topk(probs, k=k)
              idx = mx.argsort(probs, axis=-1)[-k:]
              idx = idx[::-1]
              vals = vals / mx.sum(vals)
              out = mx.zeros_like(probs)
              for j in range(k):
                  out[int(idx[j].item())] = vals[j]
              return out

          def kl_mx(p, q, eps=1e-12):
              p = mx.maximum(p, eps)
              q = mx.maximum(q, eps)
              return mx.sum(p * mx.log(p / q))

          budgets = [64, 128, 256]
          interfaces = ["argmax", "topk", "probs"]

          for budget in budgets:
              print("\n==============================")
              print("BUDGET =", budget)
              print("==============================")

              for interface in interfaces:
                  print("\nINTERFACE =", interface)
                  agreements = []
                  kls = []

                  for prompt in prompts:
                      print("\nPROMPT =", repr(prompt))
                      ids = tokenize(tokenizer, prompt)
                      print("TOKEN_IDS =", ids)
                      print("TOKEN_IDS_LEN =", len(ids))

                      x = mx.array([ids], dtype=mx.int32)
                      print("INPUT_SHAPE =", x.shape)
                      print("INPUT_DTYPE =", x.dtype)

                      out = model(x)
                      logits = extract_logits(out)
                      print("LOGITS_SHAPE =", logits.shape)
                      print("LOGITS_DTYPE =", getattr(logits, "dtype", None))

                      if len(logits.shape) == 3:
                          next_logits = logits[0, -1, :]
                      elif len(logits.shape) == 2:
                          next_logits = logits[-1, :]
                      else:
                          raise RuntimeError(f"Unexpected logits shape: {logits.shape}")

                      victim = mx.softmax(next_logits, axis=-1)
                      victim32 = victim.astype(mx.float32)
                      print("VICTIM_SUM =", float(mx.sum(victim32).item()))
                      print("VICTIM_VOCAB =", victim.shape[-1])

                      if interface == "argmax":
                          student = one_hot_from_top1(victim32)
                      elif interface == "topk":
                          student = topk_view(victim32, k=5)
                      elif interface == "probs":
                          student = victim32
                      else:
                          raise RuntimeError(interface)

                      student32 = student.astype(mx.float32)
                      print("STUDENT_SUM =", float(mx.sum(student32).item()))

                      agreement = int(int(mx.argmax(student32).item()) == int(mx.argmax(victim32).item()))
                      div = float(kl_mx(victim32, student32).item())

                      agreements.append(agreement)
                      kls.append(div)

                      top_vals = mx.topk(victim32, k=10)
                      top_idx = mx.argsort(victim32, axis=-1)[-10:]
                      top_idx = top_idx[::-1]

                      print("VICTIM_TOP10_IDS =", [int(v) for v in top_idx.tolist()])
                      print("VICTIM_TOP10_PROBS =", [float(v) for v in top_vals.tolist()])
                      print("AGREEMENT =", agreement)
                      print("KL =", div)

                  mean_agreement = sum(agreements) / len(agreements)
                  mean_kl = sum(kls) / len(kls)
                  print("\nSUMMARY")
                  print("MEAN_AGREEMENT =", mean_agreement)
                  print("MEAN_KL =", mean_kl)

          print("\nQWEN_INTERFACE_EXPERIMENT = PASS")
          PY

      - name: Final note
        if: always()
        shell: bash
        run: |
          echo "=== DONE ==="
          echo "All preliminaries printed directly to console."
